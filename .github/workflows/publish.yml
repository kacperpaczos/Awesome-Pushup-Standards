# Disabled: npm publish is out of scope (no Trusted Publisher / OIDC).
# Version tags and CHANGELOGs are managed in-repo via changesets.
name: Publish

on:
  push:
    tags:
      - v*.*.*

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "npm publish disabled — see docs/monorepo-ci.md"
